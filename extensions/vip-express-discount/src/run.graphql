query RunInput {
  discountNode {
    metafield(
      namespace: "$app:vip-express-discount"
      key: "function-configuration"
    ) {
      value
    }
  }
  cart {
    buyerIdentity {
      customer {
        numberOfOrders
        hasTags(tags: ["vip"]) {
          hasTag
        }
      }
    }
    deliveryGroups {
      deliveryOptions {
        title
        handle
      }
    }
  }
}
